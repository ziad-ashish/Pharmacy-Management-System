# إصلاحات التشغيل اليومي

أعد تشغيل البرنامج من `run.bat` لتطبيق ترحيلات الجداول. لا يتم إعادة تسعير الفواتير القديمة أو تغيير كميات المخزون بواسطة الترحيل.

## الباركود والوحدات

من **الأدوية والمخزون ← زر الباركود بجوار الصنف**، حدد اسم العبوة وعدد وحدات البيع التي تمثلها كل مسحة. مثال: باركود العلبة = 20 قرصًا، وباركود القرص = 1.
الأصناف ذات معامل تحويل أكبر من واحد تحتاج تأكيدًا صريحًا للوحدة قبل استخدام الباركود في الكاميرا أو الماسح. تغيير وحدة البيع أو المعامل يستلزم مراجعة روابط الباركود، مع الاحتفاظ بالارتباطات السابقة وعدم حذفها.
لا يمكن تغيير وحدة البيع مع وجود رصيد؛ ذلك يحتاج تحويل أرصدة منفصلًا ومدروسًا.

## الشراء والاستلام

الكمية والتكلفة في أمر الشراء تخصان **وحدة الشراء**. التكلفة المقترحة = تكلفة وحدة البيع × المعامل. الأمر يحتفظ بنسخة من أسماء الوحدات والمعامل وقت إنشائه.
أوامر الشراء السابقة تحتفظ بكمياتها وأسعارها؛ وحداتها تُستكمل من تعريف الصنف الحالي مرة واحدة. راجع الأوامر القديمة المفتوحة قبل الاستلام إذا كان تعريف العبوة تغير سابقًا.
يجب إدخال رقم الدفعة والصلاحية من العبوة لكل استلام. للدفعات المختلفة من الصنف نفسه، استلم كل دفعة على حدة بكميتها الفعلية. لا ينسخ النظام صلاحية الصنف القديمة إلى استلام جديد.

البيع يخصم من أقرب دفعة **غير منتهية**. إلغاء فاتورة جديدة يعيد الكميات إلى الدفعات التي استُخدمت فيها. الرصيد السابق غير الموزع يُمثّل كدفعة افتتاحية بنفس بياناته؛ لا يمكن استنتاج صلاحية مفقودة من الكود. راجع بيانات المخزون القديم على العبوات، خصوصًا الدفعات بلا صلاحية.
بعد وجود دفعات، يمنع تعديل إجمالي المخزون أو الصلاحية مباشرة من نموذج الصنف كي لا يتعارضا مع الدفعات.

## مسودة البيع

تُحفظ السلة والعميل والخصم وطريقة الدفع وصور وبيانات الروشتة في SQLite لكل حساب، وليس داخل ذاكرة الصفحة فقط. بعد التحديث أو إعادة التشغيل، افتح نقطة البيع لاسترجاعها.
انتظر ظهور **المسودة محفوظة** قبل إغلاق البرنامج؛ إذا فشل الاتصال تظهر رسالة واضحة. آخر تغيير لم يصل للخادم لا يمكن ضمان استرجاعه بعد انقطاع الكهرباء.
تعديلات الروشتة غير المعتمدة تُسترجع كمسودة، ولا تعتبر تصريحًا لصرف صنف خاضع للرقابة حتى تُربط بالبيع. لا يمكن لنافذة قديمة استبدال مسودة أحدث بصمت. إعادة محاولة إصدار نفس المسودة لا تنشئ فاتورة ثانية.
إذا تغير سعر صنف، يجب إعادة إضافته بالسعر الحالي قبل إصدار الفاتورة.

## الصور والنسخ

طلبات قوائم الأدوية والبحث ونقطة البيع لا تحمل الصور المشفرة كاملة. الصور تُطلب عند عرضها من مسار محمي بتسجيل الدخول، والتعديل بدون صورة لا يحذف الصورة القديمة.

من **الإعدادات ← النسخ والاستعادة**، أدخل مسار مجلد موجود على هارد خارجي أو جهاز آخر، ثم احفظ المكان واضغط **إنشاء نسخة الآن**. حفظ المسار وحده لا ينقل بيانات.
النسخ اليدوي والتلقائي يستخدمان لقطة SQLite سليمة، ثم نسخة إضافية إن حُدد مكانها، مع فحص سلامتها وسجل للحالة. فشل النسخة الإضافية لا يلغي المحلية. اختيار مجلد آخر على نفس القرص لا يحمي من تلفه. لا يوجد رفع لخدمة خارجية.

## التحقق

- `python -m unittest discover -s tests -v`
- `node tests/test_operations.cjs`
- `node tests/test_camera.cjs`

الاختبارات تستخدم قواعد ومجلدات مؤقتة. تم اختبار استرجاع السلة، مسحة علبة = 20 قرصًا، تكلفة العلبة 80، إصدار فاتورة واستلام دفعة من الواجهة على بيانات اصطناعية.
